# Information architecture

- `/` — emergency-first homepage and full restoration narrative
- `/services/` — service overview (legacy URL preserved)
- `/water-damage-restoration/`, `/flood-cleanup/`, `/flood-restoration/`, `/structural-drying/`, `/water-extraction/`, `/sewage-cleanup/`, `/mold-remediation/`, `/leak-damage/` — substantive service pages
- `/service-area/`, `/service-area/san-jose-ca/`, `/service-area/santa-clara-ca/`, `/service-area/sunnyvale-ca/`, `/service-area/cupertino-ca/` — useful priority location pages; additional cities remain referenced but ungenerated until unique content exists
- `/about/`, `/contact/`, `/privacy/`, `/terms/`, `/404.html`

Legacy redirects: `/home` → `/`; `/contact-1` and `/take-action` → `/contact/`; the opaque legacy blog slug → `/blogs/`; current named blog routes remain crawlable under `/blogs/` until content migration is complete.
