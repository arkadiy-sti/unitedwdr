import type { APIRoute } from "astro"; import { services } from "@/data/services"; import { priorityLocations } from "@/data/locations";
export const prerender=true;
export const GET:APIRoute=()=>{const base="https://www.unitedwdr.com"; const paths=["/","/services/","/about/","/contact/","/service-area/","/blogs/","/blogs/what-tools-we-use-to-dry-up-the-flood/","/blogs/professional-water-damage-mitigation-in-the-bay-area-a-real-project-breakdown/","/privacy/","/terms/",...services.map(s=>`/${s.slug}/`),...priorityLocations.map(l=>`/service-area/${l.slug}/`)]; const urls=paths.map(path=>`<url><loc>${base}${path}</loc></url>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`,{headers:{"content-type":"application/xml; charset=utf-8"}})};
